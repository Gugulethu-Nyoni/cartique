/**
 * @semantq/storefront/models
 *
 * Data models
 *
 * Phase 1: Placeholder.
 *
 * TODO: Phase 2 — Define Product, Variant, Cart, Customer models.
 */

export class Product {
    constructor(data) {
        Object.assign(this, data);
    }
}

export class Variant {
    constructor(data) {
        Object.assign(this, data);
    }
}

export class Cart {
    constructor(data) {
        Object.assign(this, data);
    }
}

export class Customer {
    constructor(data) {
        Object.assign(this, data);
    }
}
